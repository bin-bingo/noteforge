# 2026-03-17 全流程测试报告

## 测试概览

| # | 测试项 | 状态 | 说明 |
|---|--------|------|------|
| 1 | 单元测试 (test_article.py) | ✅ 通过 | 4 个断言全部通过 |
| 2 | 智能路由 | ⚠️ 条件通过 | 6/7 断言通过，本地视频文件需先存在 |
| 3 | 文章提取 (httpbin.org) | ✅ 通过 | 提取 3734 字符 |
| 4 | OCR | ✅ 通过 | 识别 "KB-Tool OCR Test 2026"，置信度 98.4% |
| 5 | VideoPipeline 导入 | ✅ 通过 | 类正常导入 |
| 6 | CLI 入口 | ✅ 通过 | `--help` 正常输出 |
| 7 | 浏览器检测 | ✅ 通过 | Chrome: `/usr/bin/google-chrome-stable`，成功访问 example.com |

**通过率: 6/7 完全通过，1 个条件通过**

---

## 详细结果

### 测试 1：单元测试 ✅
```
✅ test_detect_input_type passed
✅ test_format_article passed
✅ test_format_article_with_annotations passed
✅ test_llm_returns_empty_on_no_key passed
✅ All tests passed!
```

### 测试 2：智能路由 ⚠️
- `mp.weixin.qq.com` → `url` ✅
- `bilibili.com/video` → `video_url` ✅
- `youtube.com/watch` → `video_url` ✅
- `/etc/hosts` → `file` ✅
- `is_wechat_article` 两个断言 ✅
- **`/tmp/video.mp4` → `unknown`（文件不存在时）** ⚠️

**根因**: `detect_input_type` 对本地路径会检查文件是否存在，不存在的 `.mp4` 返回 `unknown`。
**修复建议**: 测试前创建空文件，或修改测试用例为 `open('/tmp/video.mp4','w').close()` 前置。

### 测试 3：文章提取 ✅
- 输入: `https://httpbin.org/html`（Herman Melville - Moby-Dick）
- 输出: 3734 字符结构化 Markdown
- 包含 frontmatter + `## 原文` 章节

### 测试 4：OCR ✅
- 测试图片: 白底黑字 "KB-Tool OCR Test 2026"
- 识别结果: 226 字符，置信度 98.4%，耗时 935ms
- **注意**: 测试用例使用了错误的构造参数 `mode='raw', generate_ann=False`
  - 实际签名: `OCRPipeline(engine='auto', lang='ch', use_llm=False, verbose=False)`
  - 测试脚本需更新

### 测试 5：VideoPipeline 导入 ✅
- `<class 'kb_tool.pipelines.video.VideoPipeline'>` 正常导入

### 测试 6：CLI 入口 ✅
- 两个命令: `main`（转换）和 `config`（配置管理）

### 测试 7：浏览器检测 ✅
- 系统 Chrome: `/usr/bin/google-chrome-stable`
- Playwright 成功访问 `https://example.com`，页面标题 "Example Domain"

---

## 发现的问题

1. **测试用例与实际 API 不匹配** — 测试脚本中 OCR 和部分路由测试使用了不存在的参数/期望
2. **本地视频路由** — 文件不存在时返回 `unknown` 而非基于扩展名推断

## 建议

- 更新测试脚本参数以匹配当前 API 签名
- 为 `detect_input_type` 增加对不存在文件的扩展名回退逻辑，或在测试中预创建临时文件
